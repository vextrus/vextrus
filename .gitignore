# dependencies / build output
node_modules/
.next/
out/
# generated by next dev/build; imports .next types, so tracking it breaks a fresh-clone typecheck
next-env.d.ts

# env — .env.example is the only committed env file
.env
.env.*
!.env.example

# never any incremental state (ADR-0007: staleness is unrepresentable)
*.tsbuildinfo

# test artifacts
coverage/

# python (cad/)
cad/.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

# uploaded drawings and their artifacts (VEXTRUS_STORAGE_ROOT)
.data/
