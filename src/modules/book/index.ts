/**
 * Book — the cost database. Rate books as effective-dated datasets (PWD/LGED/
 * future unified), items with kind-or-authored-exclusion, resources, rate
 * analysis, floor multiplier schemes, the SoR digitisation pipeline (ADR-0006).
 *
 * This file is the module's only public surface (ADR-0001).
 */
export {};
