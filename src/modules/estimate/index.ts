/**
 * Estimate — prices register lines against the pinned book through the unit
 * canon; owns the estimate boundary, signature, dip sample, Certificate of
 * Measured Coverage, and issued documents (server-generated PDF).
 *
 * This file is the module's only public surface (ADR-0001).
 */
export {};
