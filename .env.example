# The source of .env, not merely an illustration of it: `scripts/provision.sh`
# copies this file verbatim on every run, substituting VEXTRUS_STORAGE_ROOT with
# this checkout's path and preferring any value already exported into the
# environment. Edit a default here and every machine gets it; edit .env and the
# next provision run takes it back (ticket 15). Nothing in here is secret — the
# database is on localhost with a dev password, and BETTER_AUTH_SECRET is a
# fixed dev-only string on purpose, because a per-container random value is
# divergence nobody chose and nothing depends on it matching.
#
# App connects as the RLS-constrained role; migrations as the owner role (ADR-0004).
# better-auth connects as the auth-lane role: constrained, explicit policies only.
DATABASE_URL=postgres://vextrus_app:vextrus_app_dev_password@localhost:5544/vextrus
MIGRATE_DATABASE_URL=postgres://vextrus:vextrus_dev_password@localhost:5544/vextrus
AUTH_DATABASE_URL=postgres://vextrus_auth:vextrus_auth_dev_password@localhost:5544/vextrus
BETTER_AUTH_SECRET=dev-only-secret-rotate-in-prod
BETTER_AUTH_URL=http://localhost:3210

# Artifact storage root: uploaded sources + EntityGraph artifacts. The DB
# stores references into this tree, never blobs (ticket 04). Absolute only —
# the web server and the worker are separate processes and must resolve a
# stored reference to the same place, whatever their cwd. Point it at
# <your checkout>/.data/artifacts in dev.
VEXTRUS_STORAGE_ROOT=/absolute/path/to/vextrus/.data/artifacts
